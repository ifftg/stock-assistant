# 工作日志｜2025-09-17

## 今天完成
- 用 .env.local 的项目与密钥通过 Management API 打通远程只读/少量变更链路：
  - 列出项目成功，确认 `stock assistant (wvkrfaznogbruocaxfja)` 正常。
  - 执行只读 SQL 定位定时任务失败原因：已装 `pg_cron`，但未装 `pg_net`，导致 `net.http_post` 不存在。
  - 证据：`cron.job_run_details` 近两日均为 `failed: schema "net" does not exist`。
- 修复根因：已启用 `pg_net` 扩展并重启 worker，验证 `net.http_post` 可用。
- 新增批量日线 Edge Function 源码（待部署）：
  - 路径：`supabase/functions/daily-history-batch/index.ts`
  - 动作：
    - `fetch_history_batch`: 接收 `tickers[]`，并发抓取东方财富日线，`onConflict: ticker, trade_date` 写入 `stocks_daily`。
    - `fetch_all_daily`: 从 `stocks_info` 读全量代码并批量抓取（用于一次性/兜底）。
  - 内置轻量限速 + 并发控制（默认 12 并发）。
- 创建“每天全量 AB 股日线自动更新”的 `pg_cron` 任务（已就绪，等函数部署）：
  - 任务名：`daily-history-all`
  - 时间：`UTC 08:30`（北京时间 `16:30`），工作日 `1-5`。
  - 逻辑：将 `stocks_info` 分片（每片 250 只），逐片调用 `daily-history-batch` 的 `fetch_history_batch(days=1)`，实现全市场日线入库。
- 回补验证（以两只股票验证链路闭环）：
  - `300450`、`002606` 回补 `2025-09-16/17` 成功（`002606` 由于外键缺失先补最小 `stocks_info` 行后成功）。
  - `stocks_daily` 检查：两只个股已具备 16/17 两日数据；全市场 `max(trade_date)` 仍为 `2025-09-15`（等待全量任务）。
- 现有两个列表刷新任务保留：`daily-stock-update-batch1/2`（调用 `stock-data-fetcher` 的 `fetch_stock_list`）。

## 当前阻塞 / 风险
- Supabase Edge Remote Tools 通道仍报错（Transport is closed），无法“一键远程部署函数”。
- 你当前无法登录 Supabase Dashboard，暂时不能手动部署 Edge Function。
- `stock-data-fetcher` 本身不支持 `fetch_daily_incremental`（今天通过新函数 `daily-history-batch` 侧面实现日线全量），需最终部署成功后才能自动每日入库。

## 明日计划 / 待办
1) 部署 `daily-history-batch`（Dashboard → Edge Functions → Create → 名称 `daily-history-batch` → 粘贴 `index.ts` → 配置 Secrets → Deploy）：
   - Secrets：`SUPABASE_URL`、`SUPABASE_SERVICE_ROLE_KEY`。
   - 成功后，`daily-history-all` 任务将在每个交易日 16:30 自动触发全量 AB 股日线更新。
2) 首次全量手动触发（可用 SQL/HTTP 触发一次，等价 16:30 的定时跑），并验证当日行数 > 5000。
3) 观察 `net._http_response` 与 `cron.job_run_details`，若受限流，按需：
   - 上调分片数（缩小每片至 150-200）、
   - 降低函数内部并发（从 12 降到 8-10）、
   - 或延长 `timeout_milliseconds`。
4) 将 `daily-history-batch` 增强：
   - 支持 `date` 参数，仅拉指定交易日，避免跨天混淆。
   - 写入 `sync_runs` 进度表，记录成功/失败与失败重试列表。
5) `stocks_info` 完整性补齐：名称/市场/行业字段的正确性（依赖 `fetch_stock_list` 的日更）。

## 验收标准（全量 AB 股日线）
- 每个交易日北京时间 `16:45` 前：
  - `select max(trade_date) from stocks_daily;` = 当日日期。
  - `select count(*) from stocks_daily where trade_date = current_date;` > 5000。
  - 抽查多市场（上证/深证/科创/北交所/沪深 B 股）多代码均有当日数据。
  - `cron.job_run_details` 最新记录为 `succeeded`（或返回码/日志显示 200）。

## 自检/验证指令（Dashboard → SQL Editor）
- 扩展/任务检查：
```sql
select * from pg_extension where extname in ('pg_cron','pg_net');
select jobid, jobname, schedule, active from cron.job order by jobid;
select jobid, status, return_message, start_time, end_time from cron.job_run_details order by start_time desc limit 50;
```
- 今日数据量：
```sql
select max(trade_date) as max_date from stocks_daily;
select trade_date, count(*) rows from stocks_daily where trade_date = current_date group by trade_date;
```
- 分片调用结果（pg_net 响应摘要）：
```sql
select id, status_code, left(content::text, 200) as content, created
from net._http_response order by id desc limit 20;
```

## 需要你确认/解锁的点
- 你能登录 Dashboard 后，按“部署步骤”完成 `daily-history-batch` 的首次部署。
- 是否需要我在你登录前，用 Management API 代你“立即分片触发一次今天的全量”（不等 16:30），待你能查看后再验证行数与日志。

---
注：本日志不包含任何敏感密钥内容；真实密钥仅保存在 `.env.local` 与 Supabase Secrets。
