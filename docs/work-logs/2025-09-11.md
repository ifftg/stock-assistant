# 工作日志（2025-09-11）

## 今日完成（已推送 feat/backend-api）
- 修正涨跌幅计算口径为“昨收基准”：(close - prev_close) / prev_close
  - 已改端点：/api/stocks、/api/strategies/screen
  - 目的：修复如 300450、002606（向日葵）等出现“价格对但涨跌幅不对”的问题
- 首页仅展示真实数据：去除 includeTestData=true，避免演示数据混入
- 新闻接口改为读取数据库 news 表：/api/news 直接按 publish_time 降序读取
- AI 分析 Edge Function：将模型改为 gemini-1.5-flash（与你已在 Edge Secrets 中配置的 GEMINI_API_KEY 兼容）
- 新增市场指数更新函数源码：supabase/functions/market-indices-updater/index.ts（抓取上证/深成/创业板并写入 market_indices）
- 代码提交：7a40152（fix(data): changePercent 改为昨收基准；首页只用真实数据；新闻读 DB；AI 模型更新；新增指数更新函数源码）

## 当前状态 / 风险
- Vercel 前端：代码已推送，正常触发构建
- Supabase Remote Tools：当前通道不稳定（Transport is closed），导致暂未能通过远程工具直接“部署 Edge Functions / 执行 SQL / 批处理”。会持续重试；若持续不稳定，将采用 Dashboard 手动部署的应急流程
- 数据一致性：历史 K 线 upsert 目前未显式写入 prev_close 字段（K 线返回不含昨收，需要由前一根 close 推导）。这会影响历史 change_percent 的准确性，需在 Edge Function 中完善

## 今天发现的问题与根因追踪
1) 涨跌幅不对
   - 根因：以前用开盘价作为分母；正确应以昨收作为分母
   - 处理：后端接口改为优先使用 prev_close；缺失时回退 open
2) 首页仍为演示数据
   - 根因：includeTestData=true
   - 处理：首页请求改为 includeTestData=false，仅拉真实数据
3) 新闻未展示今日真实新闻
   - 根因：接口为“模拟生成”
   - 处理：/api/news 改为读取 DB news 表
4) AI 分析不可用
   - 可能原因：函数未部署或环境变量/模型路径不匹配
   - 处理：更新为 gemini-1.5-flash；待函数部署后验证（Secrets 已就绪）
5) 覆盖范围不够（A+B 5000+）
   - 根因：仅部分批次示范运行；全量批处理未完成
   - 处理计划：通过 stock-data-fetcher 分批（≤50/批）跑完 30 日数据

## 明日计划 / 待办清单（按优先级）
1) 用 Supabase Remote Tools（或 Dashboard 备选）部署 Edge Functions（高优先级）
   - ai-analysis（使用 GEMINI_API_KEY）
   - market-indices-updater（写入 market_indices，is_test_data=false）
2) 数据核查与纠偏（高优先级）
   - SQL 检查 300450、002606 最近 10 日：open/close/prev_close/change_percent
   - 若历史存在缩放/分母错误：按 K 线重拉 30 日并 upsert 纠偏
3) 全量覆盖 A+B 5000+ 的近 30 日数据（高优先级）
   - 使用 stock-data-fetcher：分页拉列表 → 分批（≤50）拉 K 线 → upsert
   - 记录统计：stocks_info 总数、stocks_daily 新增/覆盖行数
4) 完善 K 线 upsert 逻辑（高优先级）
   - 在 stock-data-fetcher 中：
     - 从序列计算 prev_close（用前一根 close），第一根可置 null
     - 同步写入 change_percent = (close - prev_close)/prev_close
5) 定时化（中优先级）
   - 指数：收盘后 Cron 触发 market-indices-updater（工作日）
   - 股票：每日增量日线 Cron（工作日），必要时晚间补正
   - 新闻：如无现有入库通道，新增 news-updater 函数 + Cron（来源待确认）
6) AI 分析联调与 UX（中优先级）
   - 前端错误提示完善（额度不足/密钥缺失等）
   - 将结果写入 ai_analyses，并在 UI 可回看历史
   - 次数限制与订阅联动（结合 user_profiles）
7) 首页数据校验（中优先级）
   - 指数板块改为读 market_indices 最新记录
   - 热门股票榜单口径（成交额/换手率/涨幅）明确化

## 需要你确认/提供
- 如 Remote Tools 继续不稳定，是否同意我改用 Dashboard 立即手动部署 ai-analysis 与 market-indices-updater？
- 新闻数据的来源与表结构确认（若暂无入库，请同意我新增 news-updater 函数）
- 允许创建 Cron Jobs（需要使用 Service Role 触发 net.http_post）

## 验收标准（里程碑）
- 首页指数/股票/新闻均为当日最新真实数据，无“演示数据”徽标
- 指定个股（300450、002606）价格与涨跌幅与行情源一致
- 数据库覆盖：A+B ≥ 5,000 只，均拥有近 30 日日线
- 每日 15:10 后指数自动更新；18:30 后日线增量入库；新闻按计划更新
- AI 分析可用，能生成报告并保存记录

## 参考接口 / 工具
- Edge Functions：/functions/v1/ai-analysis、/functions/v1/market-indices-updater
- API：/api/stocks、/api/strategies/screen、/api/news、/api/market-indices
- SQL 自检示例：
  - 查询：select ticker,trade_date,open_price,close_price,prev_close,change_percent from stocks_daily where ticker in ('300450','002606') order by trade_date desc limit 20;
  - 对比：select ticker,trade_date,close_price,prev_close, round((close_price-prev_close)/nullif(prev_close,0)*100,2) expected_pct, round(change_percent,2) saved_pct from stocks_daily where ticker in ('300450','002606') order by trade_date desc limit 20;

